# CLAUDE.md — Centr8 OS

Locked architecture and requirement reference for Claude Code. Read this before starting any phase. Do not deviate from the stack or patterns below without flagging it back to Urvil first.

---

## 1. Product Summary

Centr8 OS is an AI-native "operating system for work." An autonomous AI Project Manager plans, monitors, and executes operational project work. Humans set direction and approve consequential AI actions via a tiered autonomy model. Source docs: Centr8 OS BRD v1.0, Centr8 OS PRD v1.0, Centr8 OS Feature Tracker (July 2026).

---

## 2. Locked Tech Stack (Free Tier Only)

| Layer | Tool | Notes |
|---|---|---|
| Frontend / API routes + AI | Next.js (Vercel) | Standard CRUD, auth pages, dashboards, client portal, all AI agent calls inline |
| Database | Neon Postgres | Single source of truth |
| Auth / RBAC | Supabase Auth (free tier) | RLS for multi-tenant isolation |
| LLM | OpenRouter (free tier, google/gemma-4-31b-it:free) | All agent reasoning; mock mode available via `NEXT_PUBLIC_AI_PROVIDER=mock` |
| RAG / embeddings | Postgres + pgvector (Neon extension, free) | No external vector DB |
| Email / transactional | Resend | Client comms, notifications |
| Analytics | PostHog | Feature usage, AI action logs |
| PDF / doc generation | react-pdf (client-side) | Generative docs (PRDs, SOPs, reports) render client-side |
| Realtime / notifications | Postgres LISTEN/NOTIFY or polling | No paid realtime service |

**Hard constraint:** No paid infra (Temporal, ClickHouse, dedicated vector DB, paid Redis) unless explicitly revisited at a phase gate, same pattern as Recur8.

---

## 3. Multi-Tenant & Data Isolation

- Every table scoped by `org_id`. RLS policies enforced at Postgres level via Supabase Auth, not just app-layer checks.
- AI context/memory (workspace memory, RAG index) is partitioned by `org_id` — no cross-tenant leakage, including in embeddings/vector search.
- Use direct (non-pooled) Neon connection string for all migrations; pooled connection for runtime queries only.

---

## 4. AI Autonomy Tiers (Governance Model — PRD Section 5)

| Tier | Behavior | Example |
|---|---|---|
| Tier 0 — Suggest Only | AI proposes, human must trigger | Draft client comms |
| Tier 1 — Approve to Act | AI queues, human approves/rejects in a window | Sprint plan activation, reassignment |
| Tier 2 — Act with Notification | AI executes low-risk reversible actions, notifies after | Status updates, standup posts |
| Tier 3 — Full Autonomy (opt-in, scoped) | AI executes without per-instance approval, within policy | Recurring task generation, routine reports |

**Default for every new AI action type: Tier 0.** Escalating to Tier 1+ requires an explicit config flag per org, per action type. Never default an action higher than Tier 0 without Urvil confirming it in the phase review.

---

## 5. Composable Agent Pattern

Do not build one monolithic "do everything" AI call. Five specialized agents, each a distinct prompt/service, called inline from Next.js API routes via `generateAI()` (`lib/ai/generate.ts`):

- **Planner** — NL → structured project/sprint plans
- **Monitor** — health signals, risk detection, delivery prediction
- **Analyst** — comparative analysis, executive insights
- **Writer** — generative docs (PRDs, SOPs, release notes, reports)
- **Communicator** — client updates, standup summaries

---

## 6. Requirement ID Convention

Requirements referenced as `FR-x.x` (functional), matching the Feature Tracker. 63 total FRs across 13 categories. Priority: 46 Must, 16 Should, 1 Could. Full detail lives in `Centr8_OS_Feature_Tracker.xlsx` — this file summarizes scope only; the tracker is the source of truth for status/owner.

Category → ID prefix mapping:

| Category | Prefix range (approx) |
|---|---|
| Organizations, Workspaces & Access | FR-1.x |
| Work Hierarchy (Goals→Tasks) | FR-2.x |
| Resource Planning & Budgeting | FR-3.x |
| Client Portals | FR-4.x |
| Documentation & Knowledge Mgmt | FR-5.x |
| Automation, API, Webhooks, Integrations | FR-6.x |
| AI: NL Project Creation | FR-7.x |
| AI: Monitoring, Prediction, Risk | FR-8.x |
| AI: Sprint Planning & Workflow Automation | FR-9.x |
| AI: Generative Docs & Comms | FR-10.x |
| AI: Workspace Memory & RAG Q&A | FR-11.x |
| AI Assistant Interfaces | FR-12.x |
| Executive Insight & Decision Support | FR-13.x |

(Verify exact IDs against the tracker before implementing — this table is a navigation aid, not the authoritative list.)

---

## 7. Acceptance Criteria Pattern

Every feature implemented must satisfy, at minimum:
1. Scoped correctly to `org_id` (no cross-tenant data visible)
2. If it's an AI action: correct autonomy tier enforced, and an audit log entry written
3. If it's AI-generated content: a "provisional/AI-generated" banner shown until a human confirms/accepts it (same UX pattern as LucidCarat)
4. No paid service introduced without a flagged deviation

**Implementation note:** AI touchpoints must use the shared `AiButton` + `AiSuggestionCard` (+ `useAiCall`) from `components/ui/AiTouchpoint.tsx` — do not re-implement the banner + Accept/Reject/Edit pattern inline per screen. See `components/NewProjectWizard.tsx` and the HR Employee Detail page's AI Insights tab (`app/(app)/hr/employees/[id]/page.tsx`) as canonical examples.

---

## 8. Out of Scope (V1) — Do Not Build

- Native time-tracking hardware
- Payroll / full HRIS
- Full accounting/ERP (integration only, not a rebuild)
- On-prem / air-gapped deployment
- Native mobile apps

---

## 9. Open Questions (Not Yet Decided — Flag if Blocking)

- Final AI usage-based pricing model (per-seat + consumption vs. tiered flat)
- Which LLM provider/routing strategy long-term (currently Gemini free tier for build phase only — production model choice is unresolved)
- Whether native mobile gets pulled into Release 2
- Minimum viable native integrations for early design partners
- Data residency requirements for first regulated customer

---

## 10. Reusable Cross-Project Patterns (Carry Forward)

- Provisional-results banner (LucidCarat) — applies to all AI-generated output here, not just docs
- Postgres-backed job queue (RAG Scanner) — reused directly for agent task queueing
- Separate-repo admin panel pattern (ExportInvoice Pro precedent) — `centr8os-admin` as its own repo if/when an internal admin console is needed
- react-pdf async/sync boundary (ExportInvoice Pro) — applies to generative doc export (PRDs, SOPs, release notes)

---

## 11a. Scope Expansion — HR, CRM, and Communication Pillars (Added Post-V1)

Centr8 OS's scope has expanded beyond the original BRD/PRD's "AI-native project management" positioning. The product is now a multi-pillar business OS with five pillars:

1. **Project Management** (original scope — Goals→Portfolios→Projects→Milestones→Sprints→Tasks)
2. **HR Management** (new — modeled on Zoho People's feature set)
3. **CRM** (new — modeled on Zoho CRM's standard modules)
4. **Communication** (new — Messenger, Mail, Calls, Video Conferencing)
5. **AI Assistant** (cuts across all four pillars above, not a separate product)

This is a deliberate, confirmed scope change from the original BRD (which listed HR/payroll and full CRM as out-of-scope integrations-only). Anyone picking up this codebase later should treat this section as authoritative over the original BRD Section 4.2 where they conflict.

### Build vs. Integrate Decision

- **HR Management** and **CRM**: build natively, following the same schema/RLS/RBAC patterns already established for the Project Management pillar (org_id-scoped tables, `can()` permission gating, DESIGN_SYSTEM.md tokens).
- **Communication** (Messenger, Mail, Calls, Video Conferencing): integrate via connectors/plugins, do NOT rebuild natively. These are individually massive products (Slack, Gmail, Zoom-scale) and rebuilding them natively is out of scope even long-term. Use the plugin/integration architecture from Prompt 3.4 as the mechanism — Centr8 OS surfaces these tools inside its UI via connectors, it does not replace them.

### Sidebar / Navigation Structure (Locked)

```
PROJECT MANAGEMENT
  Dashboard, Projects, Sprints, Tasks

HR MANAGEMENT
  Employee Directory, Onboarding, Attendance & Time Tracking,
  Leave Management, Payroll & Compensation, Performance Reviews & OKRs,
  Recruitment / Hiring, HR Cases & Helpdesk, Learning & Training (LMS),
  Employee Engagement / Surveys

COMMUNICATION (integrated, not native)
  Messenger, Mail, Calls, Video Conferencing

CRM
  Leads, Contacts, Accounts, Deals / Pipeline, Activities,
  Sales Forecasts, Campaigns

RESOURCES
  Capacity Planning, Budgets

AI ASSISTANT (dedicated cross-module screens)
  AI Draft, Health Monitoring, Sprint Plans, Ask AI, Documents, Recommendations

INSIGHTS
  Executive Dashboard

ADMINISTRATION
  Members & Roles, SSO & Security, Automations, API Keys, Audit Log, Integrations
```

### AI Placement Rule

AI is not siloed to the AI Assistant section alone. The five composable agents (Planner, Monitor, Analyst, Writer, Communicator — CLAUDE.md §5) are reusable across all pillars:

- **HR Management**: AI-drafted job postings (Writer), AI-summarized performance reviews (Analyst), AI-flagged attendance anomalies (Monitor)
- **CRM**: AI lead scoring (Analyst), AI-drafted follow-up emails (Writer), AI-generated deal-risk summaries (Monitor)
- **Project Management**: already implemented (Health Monitoring, AI Draft)

Every contextual AI touchpoint inside a module (not a dedicated AI Assistant screen) must still follow the provisional-results banner pattern and correct autonomy tier per CLAUDE.md §4 — embedding AI into more modules does not relax the approval-gating rules.

**CRM (Batch 1 — Leads/Contacts/Accounts) is fully built.** A real CRM implementation predates this batch (accounts/contacts/leads/deals/activities/campaigns/forecasts tables, permissions, API routes, admin-console-style UI — apparently from earlier "Prompt 6.1-6.3" work never documented here). CRM Batch 1 extended leads/contacts/accounts/activities substantially — richer field sets (leads: score+reasoning, source/source_detail, campaign attribution; accounts: full address+revenue+type/status; contacts: decision-maker/primary flags, last-contacted tracking), lead conversion as a proper transactional operation (`convertLead()`, `lib/api/crm.ts` — creates account+contact, updates the lead, logs a conversion activity, all inside `withOrgContext`'s single transaction; a 400 on re-converting an already-converted or lost lead, never a silent no-op), and a shared activity timeline (`activities` table, polymorphic `relatedType`/`relatedId` across lead/contact/account) — and left deals/campaigns/forecasts untouched, out of scope for CRM Batch 2/3. Resource types: `lead`/`contact`/`account`/`activity`. Tiering: owner/admin/member get full create/read/update/delete on all three plus activity create/read (the existing flat grid, unchanged); `lead:convert` is additionally granted to member (the "Editor" tier — can progress and convert leads, not delete or reassign them); `lead:assign`/`account:assign`/`contact:assign` are owner/admin only (migration `0092_seed_crm_batch1_permissions.sql`). Seven AI touchpoints: score_lead, enrich_lead, summarize_account, summarize_contact (Analyst), suggest_lead_action, suggest_account_action (Planner), draft_crm_email (Writer) — all through the shared `AiButton`+`AiSuggestionCard`/`useAiCall` pattern, none auto-submitting. UI: `/crm` (dashboard — real KPIs from `/api/crm/stats`, recent leads, recent activities, a Pipeline Summary placeholder), `/crm/leads` (table + Kanban board with HTML5 drag between new/contacted/qualified/unqualified, lead detail panel with the three AI touchpoints and a Convert-to-Account confirmation flow), `/crm/accounts` (list) + `/crm/accounts/[id]` (tabs: Overview/Contacts/Activities/Deals-placeholder/AI Insights), `/crm/contacts` (list + detail panel, including the draft-follow-up-email touchpoint that copies to clipboard rather than sending), `/crm/activities` (read-only cross-entity log). Deals/Campaigns/Forecasts already existed from the earlier undocumented work and remain out of scope for this batch — their API routes (`/api/deals`, `/api/campaigns`, `/api/forecasts`) still function untouched, but their pages (`/crm/deals`, `/crm/campaigns`, `/crm/forecasts`) are now simple "Coming in CRM Batch 2/3" placeholders, since the sidebar no longer routes users to them for interaction. Planned: CRM Batch 2 (Deals/Pipeline) and CRM Batch 3 (Forecasts/Campaigns) will build real UI against those existing APIs.

**CRM Batch 2 (Deals/Pipeline) is now fully built.** The `deals` table was extended in place with the full pipeline field set (7-stage `deal_stage` enum: prospecting→discovery→proposal→negotiation→contract_sent→won/lost — won/lost are terminal outcomes, not Kanban columns), and a new `deal_stage_history` table tracks every transition (from_stage/to_stage/changed_by/duration_in_previous_stage_minutes), written transactionally alongside a `status_change` activity by `changeDealStage()` (`lib/api/crm.ts`) — never a plain field edit. `deal:close` is a distinct permission from `deal:update`, same reasoning as `lead:convert`: closing a deal (won/lost, sets actualCloseDate/lostReason/wonNotes via `closeDeal()`, which rejects a lost close with no `lost_reason` as a 400) has financial/reporting implications, so it isn't implied by ordinary field-edit access — `deal:assign` is likewise separate from `deal:update`. Tiering: owner/admin get all deal permissions including close/assign/delete; member ("Editor" tier) gets create/read/update/close but not delete/assign; viewer is read-only. Lead conversion can now optionally create a deal in the same transaction — `convertLead()`'s `createDeal` option (default `true`) creates account+contact+deal atomically and returns `{lead, account, contact, deal}`; the lead-conversion modal on `/crm/leads` now has an "Also create a deal" toggle (default on) wired to it. Four new AI touchpoints: assess_deal_risk (Monitor), draft_deal_proposal (Writer), suggest_deal_next_step (Planner), predict_deal_close (Analyst) — same `AiButton`+`AiSuggestionCard`/`useAiCall` pattern, none auto-submitting. UI: `/crm/deals` (KPI cards from `/api/crm/deals/pipeline-stats`; Kanban default view with optimistic drag-and-drop between the 5 open stages and revert-on-failure; Table view; Forecast view — a stacked bar by stage plus a plain-div monthly bar list, no charting library) and `/crm/deals/[id]` (5 tabs: Overview/Activities/Stage History-as-visual-timeline/Contacts/AI Insights, plus Mark Won/Mark Lost/Assign quick actions). The account detail page's Deals tab (`/crm/accounts/[id]`) now renders the account's real deals instead of the Batch 1 placeholder. Sales Forecasts and Campaigns remain out of scope (CRM Batch 3).

**CRM Batch 3 (Sales Forecasts, Campaigns) is now fully built, completing the CRM pillar — all 7 CRM sidebar sub-items are now live** (Leads, Contacts, Accounts, Deals/Pipeline, Activities, Sales Forecasts, Campaigns — none show "Coming soon" anymore). Forecasts are computed live from `deals` at read time (`computeForecast()`, `lib/api/crm.ts` — pipeline/weighted/committed/won values plus a by-stage breakdown, filtered by `expectedCloseDate` range and optionally `ownerId`), never stored as a snapshot; a separate `forecast_targets` table holds only manually-set quota targets (period/period_type/period_start/period_end/target_value, `ownerId` null = org-wide target, non-null = per-rep). `forecast:set_target` is a distinct permission from `forecast:read` — same tiering rationale as `deal:close`, a manager/admin-level action not implied by ordinary read access. Campaigns were extended in place with the full field set (type/status/budget/channel/target_audience/etc.) and their metrics (leads/deals/revenue/ROI) are always computed live through two attribution paths (`computeCampaignMetrics()`, `lib/api/crm.ts`): `leads.campaignId` set directly, or `deals.convertedFromLeadId` pointing at a campaign-attributed lead (so a deal created by converting a campaign lead still counts even though the deal itself has no `campaignId`). `campaignRoi(revenueWon, budgetSpent)` returns `null` when spend is 0 rather than dividing by zero — every UI surface checks for null before rendering a percentage. `campaign:create/update/delete` are tightened to admin-only, a deliberate retightening from CRM Batch 1's flat grant that would otherwise have given "member" full campaign CRUD. UI: `/crm/forecasts` (period-type toggle monthly/quarterly/annual, owner filter, 5 summary cards including a color-coded Gap-to-Target, a Batch-2-style CSS bar trend chart across the last 6 periods, deals-closing-this-period table, by-rep breakdown table) and `/crm/campaigns` (KPI cards from `/api/crm/campaigns/stats`, a card grid with real computed metrics per campaign and a budget-spent progress bar) + `/crm/campaigns/[id]` (Overview/Leads/Deals/AI Insights tabs; Leads tab includes a funnel summary from total leads through won deals; no delete action in the UI since `app/api/crm/campaigns/[id]/route.ts` has no DELETE handler and was out of scope to add one to). The lead creation modal (`/crm/leads`) gained an optional Campaign dropdown (active campaigns only) that sets `leads.campaignId` on create — `app/api/crm/leads/route.ts`'s POST insert was missing `campaignId` entirely despite the column existing since Batch 1, a one-line fix. Five new AI touchpoints: analyze_forecast, analyze_campaign (Analyst), suggest_pipeline_actions, suggest_campaign_improvements (Planner), draft_campaign_copy (Writer, subject can be null for social/paid_ads types) — same `AiButton`+`AiSuggestionCard`/`useAiCall` pattern, none auto-submitting. This closes out CRM Batch 1-3 (Leads/Contacts/Accounts, Deals/Pipeline, Forecasts/Campaigns) as the full native CRM pillar scope from this section's original Phase 6 plan.

### Current Status (flag for future sessions)

As of this scope expansion, Communication remains integration-only per the section above (no native build). CRM is not planning-stage — see the CRM Batch 1 paragraph above for its actual status (Leads/Contacts/Accounts rebuilt; Deals/Campaigns/Forecasts pre-existing from earlier undocumented work, out of scope for this batch). HR Management is now fully built out (Prompts 5.1–5.4), all ten sidebar items real:

- Employee Directory + Onboarding (5.1) — `employees`/`onboarding_workflows`, `employee:{create,read,update,delete,terminate}`, `/hr/directory` list/detail (now `/hr/employees`, HR Batch 1), onboarding gated by HR admin OR the employee's direct manager (needs a linked login, per employees.userId).
- Attendance & Time Tracking (5.2, **restructured for self-service in HR Batch 2** — see the note below; this bullet describes the current shape, not the original 5.2 build) — `attendance_records` (`work_date`/`check_in_time`/`check_out_time`/`total_minutes`/`status`/location+manual-entry+audit fields) + `attendance_settings` (per-org workday/weekend/late-threshold config), `attendance:{record_own,view_own,view_all,edit_any}`, `/hr/attendance` (My Attendance + Team Today views, calendar heatmap, KPIs) + `/hr/attendance/settings` + a global check-in/out widget in the top bar (`components/hr/AttendanceWidget.tsx`).
- Leave Management (5.2, **restructured for self-service in HR Batch 2 Part 2** — see the note below; this bullet describes the current shape, not the original 5.2 build) — `leave_types` (category: name/color/is_paid/requires_approval/max_consecutive_days) + `leave_policies` (allotment rule attached to a type: annual_allotment_days/applies_to/carry_forward/effective_from) + `leave_balances` (stored, lazily created per employee/type/year — allotted/carried_forward/used/pending) + `leave_requests` (total_days/is_half_day/reason/status/reviewed_by/cancellation fields), `leave:{request_own,view_own,approve,view_all,configure,manage_balances}`, `/hr/leave` (My Leave / Approvals / Team Calendar / Policies tabs) + a Leave tab on Employee Detail.
- Payroll & Compensation (5.3, **extended in HR Batch 2 Part 3 for payslip generation** — see the note below; this bullet describes the current shape) — `compensation_records` (extended in place with `pay_frequency`/`deductions`, force-RLS added) + new `payslip_records` (record-keeping payslip snapshots: gross/deductions/net, `draft → finalized → paid` lifecycle, unique per employee/period so re-generation can't duplicate), `compensation:{create,update,delete,view_sensitive}` + new `payroll:{generate,finalize,mark_paid}`, `/hr/payroll` (period selector, generation, bulk finalize/mark-paid, PDF export via `@react-pdf/renderer`) + a rebuilt Employee Detail Compensation tab (current-record card, read-only history, payslip records section).
- Performance Reviews & OKRs, Recruitment/Hiring, HR Cases & Helpdesk, Learning & Training, Employee Engagement/Surveys (5.4) — `performance_reviews`+`okrs`, `job_postings`+`candidates`, `hr_cases`, `training_courses`+`training_completions`, `engagement_surveys`+`survey_responses`. Permissions consolidated by module, not by table — `performance`/`recruitment`/`hr_case`/`training`/`engagement` resourceTypes, each reusing the existing create/read/update/delete actions (no new permission_action values). `/hr/cases`, `/hr/learning`, `/hr/engagement` remain plain CRUD, no AI. **Performance Reviews & OKRs and Recruitment/Hiring were fully rebuilt in HR Batch 3** (superseding the plain-CRUD 5.4 build for those two modules specifically) — see the two paragraphs below for their access models and AI touchpoints. `review`/`okr` and `recruitment` are the resourceTypes involved: `review:{submit_self,submit_manager,view_own,view_team,view_all,configure}`, `okr:{create_own,create_team,view_own,view_team,view_all}`, `recruitment:{read,create_job,manage_candidates,schedule_interview,submit_feedback}`. `/hr/reviews` (My Reviews / Team Reviews / All Reviews / Cycles tabs) and `/hr/okrs` (My OKRs / Team OKRs) cover the review side; `/hr/recruitment` (Job Postings / All Candidates) plus `/hr/recruitment/[job_id]` (job detail, drag-and-drop candidate pipeline, candidate detail panel with interview scheduling and feedback) cover hiring. Six AI touchpoints: draft_self_assessment (Writer), summarize_review_feedback (Analyst), suggest_key_results (Planner), draft_job_posting (Writer), summarize_candidate (Analyst), suggest_interview_questions (Planner) — all through the shared `AiButton`+`AiSuggestionCard`/`useAiCall` pattern, none auto-submitting.
- Also added: `/hr/dashboard` (real stats only — Total Employees, Onboarding, Pending Leave Requests, Checked In Today, a native 7-day attendance chart; never fabricated "Total Applicants"/"Total Projects"-style numbers) and `/admin/members` (Members & Roles — invite/role-change/deactivate, gated on `organization:update`, no new permission type needed).

**Deviation from Prompts 5.2/5.3 as originally written, confirmed by Urvil:** HR Management launched with no employee self-service login path — every module (attendance, leave, compensation, and all five 5.4 modules) was HR-admin data entry/view only, an HR admin (owner/admin role) picking an employee from a dropdown and recording/viewing on their behalf, with no member/viewer default grant anywhere in HR Management. `compensation`'s self-view fallback (5.3's acceptance criteria originally required employee self-view) was removed from `requireCompensationViewAccess` (`lib/api/employees.ts`), and 5.4's five new resourceTypes were seeded owner/admin-only from the start rather than following the "member gets blanket read" default most other resourceTypes get. Onboarding and leave-approval manager-check paths (`requireEmployeeManageAccess`/`requireLeaveApproveAccess`) were left intact — a manager with a linked login can still approve their own reports' onboarding/leave; that wasn't part of this restriction.

**Reversed for Attendance specifically in HR Batch 2 Part 1 (confirmed by Urvil):** the blanket "no self-service" policy above was a scope-cut for speed during the early HR passes, not a permanent product philosophy — it contradicted the BRD's own goal of reducing administrative overhead once real usage would mean an admin manually punching every employee's clock. Attendance now has genuine employee self-service: `attendance:record_own`/`view_own` are granted to every role (including viewer) by default, an employee checks themself in/out via the global widget or `/hr/attendance`, and `attendance:view_all`/`edit_any` (owner/admin-only) cover HR oversight and manual backfill/correction — the old admin-only `attendance:record` action and its owner/admin-only grant (migration `0035_restrict_hr_self_service_to_admin.sql`) were replaced outright, not kept alongside the new model.

**Reversed for Leave specifically in HR Batch 2 Part 2 (confirmed by Urvil), same reasoning as Attendance above:** Leave Management now has genuine employee self-service too. `leave:request_own`/`view_own` are granted to every role (including viewer) by default — an employee picks a leave type, date range (or half-day), and optional reason, and submits their own request via `/hr/leave`'s "Request Leave" modal or the Employee Detail Leave tab. Approval stays manager/HR-admin only: `leave:approve` requires the caller to actually be the requester's manager (`isManagerOf`, `lib/api/employees.ts`) or hold `leave:view_all` — the grant alone isn't sufficient, same "manager isn't a role" pattern documented for onboarding/leave-approval above, now formalized as `requireLeaveApproveAccess` in `lib/api/leave.ts`. `leave:configure` (types/policies) and `leave:manage_balances` (manual balance corrections, always audit-logged) remain owner/admin-only. The old admin-only `leave:request` action and its owner/admin-only grant were replaced outright, along with the underlying schema — `leave_policies` used to double as "the type" (just a name + days/year, no separate leave_types table, balance computed live on every read); it's now split into `leave_types` (the category) + `leave_policies` (the allotment rule) + `leave_balances` (a real stored per-employee/type/year row, lazily created on first request), restructured in place rather than built as a second parallel system — the one real "PTO" policy that existed pre-Batch-2 was migrated into a `leave_type`+`leave_policy` pair rather than discarded (migration `0077_leave_self_service_restructure.sql`).

**Compensation and all five Prompt 5.4 modules remain HR-admin-only** — both self-service reversals (Attendance, Leave) were scoped to those two modules alone; whether to extend self-service to any 5.4 module is a separate decision Urvil hasn't made, not something to assume from either precedent.

**Performance Reviews (HR Batch 3) is a genuinely distinct third access pattern, not a variant of either precedent above.** Attendance/Leave are full self-service (the employee owns the whole record end-to-end); Compensation is zero-self-service (admin-only, no employee access to any part of it). Reviews split a single row into three independently-gated tiers instead: an employee owns `self_assessment` (`review:submit_self` + the review's `employee_id` must resolve to their own linked `employees` row — checked by `requireReviewSelfAccess`, `lib/api/reviews.ts`), their manager owns `manager_assessment`+`final_rating` (`review:submit_manager` + `isManagerOf`, the same "grant alone isn't sufficient" shape as `requireLeaveApproveAccess` — checked by `requireReviewManagerAccess`), and HR admin can configure cycles and see everything (`review:configure`, `review:view_all`). The two writes are hard-separated at the route level (`PATCH /api/reviews/[id]/self-assessment` vs. `manager-assessment`), so there's no code path where an employee's self-assessment submission could touch the manager's fields or vice versa — verified directly in `db/test-reviews-batch3-verify.ts`. `review:view_own`/`view_team`/`view_all` layer three more read tiers on top (own review, direct reports via `isManagerOf`, org-wide). OKRs follow a lighter version of the same idea — `okr:create_own` for your own objective, `okr:create_team` for team-level or someone else's, `okr:view_own`/`view_team`/`view_all` mirroring the review read tiers.

**Recruitment/Hiring (HR Batch 3) has no candidate-facing portal at all** — candidates aren't Centr8 OS users, so unlike Reviews' three-way split, access here is entirely internal-staff: `recruitment:read` for visibility, `recruitment:create_job`/`manage_candidates`/`schedule_interview` grid-granted to whichever roles hold them (owner/admin/member, i.e. "hiring manager can be any role," not a fixed HR-admin-only gate), and `recruitment:submit_feedback` layers on a specific-assignment check (`requireInterviewFeedbackAccess`) — holding the grant isn't enough, the caller must be the exact interviewer named on that interview's `interviewer_id`, verified against their own linked `employees` row. A different interviewer, even one with `submit_feedback`, is still denied for someone else's interview — the client doesn't try to replicate this check, it just submits and surfaces the resulting 403.

**Payroll & Compensation specifically is a permanent, deliberate scope boundary — not a "not yet built" gap, confirmed explicitly when HR Batch 2 Part 3 (payslip generation) was commissioned.** Unlike Attendance and Leave, there is no planned self-service reversal here: `compensation:*` and `payroll:*` are Admin-only (owner/admin), full stop, with zero grant to member/viewer and no self-view fallback for an employee to see their own salary. This module is, and is meant to stay, structured record-keeping only — no tax withholding, no statutory computation (PF/ESI/TDS), no bank disbursement, no payroll-provider integration. If a future prompt proposes self-service or compliance/tax logic for this pillar, that is a scope change requiring explicit confirmation, not an extension of the Attendance/Leave pattern.

**HR Cases & Helpdesk, Learning & Training, and Employee Engagement/Surveys (HR Batch 4) are now fully built, closing out all 10 HR Management sidebar sub-items.** Each is a fourth (or further) distinct access-pattern variant on top of the three already documented above (Attendance/Leave full self-service, Compensation zero-self-service, Reviews' hybrid split): HR Cases is full self-service raise (`hr_case:create_own`/`view_own`, every role) + admin-managed resolution (`hr_case:manage`) — a case is cleanly separable per-record into "the raiser's side" and "the handler's side" rather than split within a single row the way Reviews is. Training is self-service consumption (`training:read`/`enroll_own`/`view_own`, every role) + admin-only authoring and oversight (`training:manage`/`view_all_progress`). Surveys is self-service response (`engagement:respond`/`view_own`, every role) + admin-only authoring/results (`engagement:manage`/`view_results`) — and here the anonymity guarantee is structural, not a UI convention: an anonymous response's `survey_responses` row never has `employee_id` populated (enforced in `submitResponse`, `lib/api/surveys.ts`), duplicate-submission prevention is handled by a separate `survey_respondents` table that always carries the real `employee_id` but is never selected alongside `survey_responses.answers` anywhere in the codebase, and `db/test-surveys-batch4-verify.ts` verifies this directly (null `employee_id` on the response row, a real `employee_id` on the respondent row, a 409 on re-submission, and that the exact select shape the results route uses never carries an `employee_id` key). UI: `/hr/cases` (My Cases / All Cases / Categories tabs, confidential-case redaction respected client-side per what the API already redacts), `/hr/training` (Course Catalog / My Learning / Progress Overview — Learning & Training's route path, replacing the old `/hr/learning`), `/hr/surveys` (Active Surveys / Manage Surveys / Results — replacing the old `/hr/engagement`). Six AI touchpoints: suggest_case_triage, suggest_case_resolution (Analyst), recommend_courses_for_employee (Analyst), generate_course_outline (Writer), suggest_survey_questions (Planner), summarize_survey_results (Analyst) — all through the shared `AiButton`+`AiSuggestionCard`/`useAiCall` pattern, none auto-submitting.

**AI Assistant section is now fully built — all 6 sidebar sub-items are live** (AI Draft, Health Monitoring, Sprint Plans, Ask AI, Documents, Recommendations — none show "Soon" anymore). Sprint Plans (`/ai/sprint-plans`) is the Planner agent's autonomous sprint planning, a genuine Tier 1 — Approve to Act action (CLAUDE.md §4): `sprint_plan_proposals` stores AI-drafted proposals (`pending`/`approved`/`rejected`/`expired`), and only `POST /api/ai/sprint-plans/[id]/approve` — never `generate/route.ts` — writes a real `sprints` row + its `tasks` (via `approveSprintPlan()`, `lib/api/aiAssistant.ts`); rejecting persists a `rejection_reason` and creates nothing. `sprint_plan:{read,create,approve}` — read is every role, create/approve are owner/admin/member (this app's stand-in for "PM, admin", same tier `lead:convert`/`deal:close` got in CRM). Ask AI (`/ai/ask` full page + a floating widget wired into `components/AppShell.tsx`'s existing header button, `components/ai/AskAiChat.tsx` shared between both) is real workspace-memory Q&A (FR-11.x) backed by `ai_conversations`+`ai_messages` — no dedicated resourceType, RLS alone scopes each user to their own conversations (`user_id = auth.uid()`), verified directly in `db/test-ai-assistant-verify.ts` (a second user gets zero rows back, not a 403). Answers stream in word-by-word client-side (the mock still returns the full answer in one shot — there's no real token stream yet) and carry citation chips; the floating widget and the full page share the same active conversation via a `localStorage` key so switching pages doesn't lose context. Documents (`/ai/documents` + `/ai/documents/[id]`) is the Writer agent's generative-doc engine (FR-10.x): `generated_documents` with a one-way `draft → reviewed → finalized` lifecycle — `reviewDocument()` can go either direction between draft and reviewed, but `finalizeDocument()` is irreversible by design and locks further edits (`editDocument()` and reverting both reject once finalized, verified in the regression test), PDF export reuses the Payroll batch's `@react-pdf/renderer` setup. `document:{read,create,update,finalize}` — read is every role, create/update (generate/edit/mark-reviewed) are owner/admin/member, finalize is owner/admin only. Recommendations (`/ai/recommendations`, plus a top-5 compact version replacing the Executive Dashboard's old hardcoded `MOCK_RECOMMENDATIONS` placeholder) needs no table — computed live every request from real overdue-tasks/at-risk-projects/over-allocated-people/stale-deals/pending-leave/pending-sprint-plan signals across the org, no permission gate (scoped only by what the caller's own resource-level grants already let the underlying queries see). All 4 new mock functions (`generate_sprint_plan`/Planner, `ask_ai`/Analyst, `generate_document`/Writer, `generate_recommendations`/Analyst) live in `lib/ai/mockResponses.ts` behind the same `generateAI()` router as everything else — switching `NEXT_PUBLIC_AI_PROVIDER` to a real LLM provider requires zero UI changes, per this batch's build spec.

**Global cross-pillar dashboard added at `/dashboard` — now the default post-login landing page** (`app/page.tsx`'s `/` redirect and the login page's post-auth redirect both already pointed at `/dashboard`, unchanged; what changed is what lives at that URL). This is a genuinely new, separate page from the old PM-scoped dashboard — that page was moved in place to `/projects/dashboard` (`app/(app)/projects/dashboard/page.tsx`, content untouched) and the Project Management sidebar section's "Dashboard" item now points there instead. A new standalone "Dashboard" link (grid icon, `ICON.grid`) sits above every pillar section in the sidebar, not inside any of them — it's the org-wide command center, not a PM feature. `GET /api/dashboard` (`lib/api/dashboard.ts` — the route itself is a thin wrapper, same pattern as `lib/api/aiAssistant.ts`) aggregates projects/tasks/sprints, employees/attendance/leave/HR-cases, leads/deals/accounts, mocked Communication stats (`lib/mock/communication.ts` — no real connector exists per CLAUDE.md §11a), AI (pending sprint plans + draft documents), and a merged `recent_activity` feed (audit_log ∪ CRM `activities`, since CRM's timeline never wrote to audit_log). No dedicated resourceType or permission gate on the endpoint itself — each section independently checks the same resourceType:action its own pillar page already requires (`project:read`, `employee:read`, `lead:read`, `attendance:view_all`, `leave:view_all`, `hr_case:manage` for HR-admin oversight since `hr_case` has no plain `read` action, etc.) and resolves to `null` rather than a zeroed block or a 403 on the whole request — verified directly in `db/test-dashboard-verify.ts` (a custom no-grant role gets `leads`/`deals`/`accounts` as `null` while `communication`/`recent_activity`, which are never gated, still come back populated). New mock function `daily_briefing` (Analyst) powers the dashboard's "AI: Daily Briefing" card, a 3-5 sentence cross-pillar summary + typed highlights (`positive`/`warning`/`action_needed`), dismissible per-day via a dated `localStorage` key.

**Dashboard visually redesigned** (post-initial-build): Quick Stat cards gained real per-category mini-bar sparklines (e.g. the Tasks card's bars are pending/in_progress/in_review/completed counts, not decoration) plus real period-over-period trend pills on Tasks (`tasks.completed_trend_percent`, week-over-week completions) and Pipeline (`deals.value_trend_percent`, month-over-month deal value) — both computed server-side in `lib/api/dashboard.ts`, both `null` (no pill rendered) rather than fabricated when there's no prior-period data to compare against. `GET /api/dashboard` gained `recent_tasks_list` (top 5 open tasks by soonest due date, with `people` joined for a real assignee name — `tasks.assigneeId` is a bare uuid into `people`, same no-FK convention as every other actor reference in this schema) rendered as a real "Recent Tasks" table. The AI Recommendations card was replaced with an embedded, fully-functional Ask AI chat panel (reusing `components/ai/AskAiChat.tsx` — the same chat as `/ai/ask` and the header widget, not a decorative mockup) with the top-3 real recommendations rendered as quick-tiles below the input. A "Next Meeting" card (dark, pulls from `lib/mock/communication.ts`'s `nextMeeting()`) sits above the chat panel. The two-column section grid needed `items-start` — without it, CSS Grid's default `align-items: stretch` force-stretched the shorter column's box to match the taller one, leaving a visible blank gap (a real bug, caught from a user screenshot, not a screenshot-tool artifact — confirmed via DOM inspection before fixing). Recent Activity is deliberately the last block in its column (explicit request) since it's the longest one.

**TODO (unresolved, flag if picked up in Batch 5+): `employees` vs `people` dual-directory.** PM's Team Directory (`people` table, resourcing-focused) and HR Management (`employees` table, HR-record-focused) are two separate, unlinked tables describing overlapping sets of humans. HR Batch 1 (Employee Directory + Onboarding rebuild) deliberately extended `employees` in place rather than merging into `people`, after an audit found 9 live FKs from already-built HR modules (attendance, leave, compensation, performance, recruitment, cases, training, engagement) pointing at `employees.id` — retargeting all of them was out of scope for that batch. This was an explicit, confirmed-with-Urvil scope decision, not an oversight: the merge is intentional-later, not forgotten. Before building anything that assumes one unified person record (e.g. a cross-pillar people search, or letting a PM project pull HR fields), re-raise this with Urvil rather than assuming either table is canonical.

**In-app Notifications system added** — a bell icon in the top bar (`components/notifications/NotificationBell.tsx`, wired into `components/AppShell.tsx` next to the existing "Pending approvals" shield icon) is genuinely new and deliberately kept separate from that shield icon: the shield is a mocked-up stand-in for the future Tier-1 AI-approval queue (still hardcoded local state, no backend — see the comment at `AppShell.tsx`'s `approvals` state), unrelated to real notifications, confirmed with Urvil not to merge the two. Schema: `notifications` (`org_id`/`user_id`/`type`/`title`/`body`/`icon`/`link_type`/`link_id`/`is_read`/`read_at`, index on `(user_id, is_read, created_at)`, migration `0102_add_notifications.sql` — hand-written, not drizzle-kit-generated, same reason as 0099's header comment (the local drizzle snapshot chain stopped at `0085_snapshot.json` while the journal is at 101+, so `db:generate` pulls in years of unrelated drift; `db:generate`/`db:migrate` also can't run non-interactively in a piped/CI shell — drizzle-kit's rename-vs-create prompt requires a real TTY — so future schema changes on this table need the same hand-write-the-SQL treatment, not `db:generate`). RLS is **split per-action** (`notifications_select`/`_insert`/`_update`/`_delete`), unlike most tables' single `for: all` policy: insert only checks org membership of the *actor*, while select/update/delete require `user_id = auth.uid()` (the *recipient*) — because a notification's actor and recipient are almost always different people (a manager notifying a requester), and a single combined policy would block the INSERT since WITH CHECK would fail for the recipient's user_id. **Corollary bug this caused and fixed:** `INSERT ... RETURNING` also has to satisfy the SELECT policy on the row it returns — since the actor usually isn't the recipient, `RETURNING` would fail RLS even though the INSERT itself is allowed. `lib/notifications/create.ts`'s `createNotification()` works around this by generating the id client-side (`randomUUID()`) and inserting without `.returning()`, rather than reading the row back. No dedicated resourceType — RLS alone scopes reads to the recipient, same reasoning as `ai_conversations`.

Real event wiring — 8 types wired end-to-end across all four pillars (chosen because each has a clean, resolvable recipient `user_id`; see the blockers paragraph below for what wasn't wired): `leave_request_pending` (manager, on submit — `app/api/leave/request/route.ts`, replacing what used to be a `console.log` stub), `leave_approved`/`leave_rejected` (requester, on the approve/reject routes), `hr_case_update` (case raiser, on status change — `app/api/hr-cases/[id]/route.ts`, only fires when status actually changes), `lead_assigned` (the newly-assigned rep — `app/api/crm/leads/[id]/assign/route.ts`), `deal_won` (deal owner, inside `closeDeal()` in `lib/api/crm.ts` itself rather than its route, so every caller gets it for free), `document_ready` (the requester, inside `app/api/ai/documents/generate/route.ts` — synchronous today so this fires in the same request/response cycle, ahead of any future async job queue), `sprint_plan_pending` (every org member holding `sprint_plan:approve`, i.e. owner/admin/member — queried live from `org_memberships` since sprint plans have no single "approver" field). Every wiring site resolves an `employees.id` (or `orgMemberships`) row to an actual `auth.users` id via `employees.userId` — never assumes the id in scope is already a login id.

**Known blockers, not wired (flag before extending):** `task_assigned`/`task_overdue` were scoped out — `tasks.assigneeId` is a bare uuid into `people`, and `people` has no `userId` column or any FK to `employees`/`auth.users` at all (confirmed: no resolution path exists), unlike every other "assignee" field in the app which resolves through `employees.id → employees.userId`. Wiring task notifications requires adding that link first — a schema decision for Urvil, not a wiring gap. `task_overdue`, `deal_stage_changed` (stale >14 days), and `ai_recommendation` (critical-priority) all need a **dedup/"notify once" mechanism** that doesn't exist yet — there's no cron/background job anywhere in this repo (CLAUDE.md §2, no paid infra) so these can only be computed at read time, and the existing read-time computation for staleness (`app/api/ai/recommendations/route.ts`'s `STALE_DAYS` filter) and for recommendations (regenerated live by the LLM every call, no stable id) has no persisted "already notified" state — adding one (e.g. a `staleNotifiedAt` column, or a fingerprint table) is a small design task of its own, deliberately left out of this pass rather than half-built.

UI: the bell dropdown (header/gear-link-to-settings/scrollable list capped at 8/footer with Mark-all-read + View-All, `components/notifications/NotificationBell.tsx`) polls unread count on window focus + a 60s interval (no websockets, per the "keep simple" spec) and fetches the list fresh each time it opens. `/notifications` is the full page — filter tabs (All/Unread/Project/HR/CRM/AI, the last four keyed off `notificationMeta().category` in the shared `components/notifications/shared.ts` module), date-grouped sections (Today/Yesterday/This week/Earlier, same grouping shape as the dashboard's `groupByDate`, duplicated rather than shared per this repo's existing `timeAgo()` convention), pagination via a Load More button, and checkbox-based bulk mark-read/dismiss (client-side `Promise.all` over the single-item endpoints — no dedicated bulk API route, since the per-item routes were already RLS-safe and sufficient). `components/notifications/shared.ts` (icon path + `BadgeColor` + category per notification type, plus `linkFor()` resolving `link_type`/`link_id` to a route) is the one shared module in this feature — deliberately shared (unlike `timeAgo()`) because the bell dropdown and full page must render the exact same icon/color per type, a cross-surface consistency need `timeAgo()` doesn't have. Regression test: `db/test-notifications-verify.ts` — cross-user isolation (User A can't read or mark-read User B's notification), `createNotification()` insert-then-immediately-queryable, and mark-all-read scoped to the caller only (not org-wide).

**ClickUp integration added — the first real, non-mocked connector besides Slack/Gmail, and the first to use a Personal API Token instead of OAuth.** Part 0 investigation found the `integrations` table, RLS, and `toPublicIntegration()` secret-stripping pattern already existed and were real (not a mocked shell) — Slack/Gmail already had working OAuth connect/callback/disconnect flows. ClickUp **extends that existing table and pattern** rather than adding a parallel one: `integrationProviderEnum` gained a `'clickup'` value (migration `0103_add_clickup_integration.sql`, hand-written for the same reason as `0102` — the local drizzle snapshot chain is years behind the applied-migrations journal, so `db:generate` is unsafe here), and the token/team id/team name live in the same plaintext `config` jsonb column Slack's `access_token`/`team_id`/`team_name` already use — this repo has no reversible-encryption helper anywhere (confirmed by a full grep), so "never returned to the client" is enforced the same way it already was: `toPublicIntegration()` (`lib/api/integrations.ts`) strips `config` entirely before any response ever serializes a row, and `db/test-clickup-integration-verify.ts` asserts the raw token string is absent from that output, not just "probably fine." Two new nullable columns, `lastSyncedAt`/`lastError`, were genuinely new (Slack/Gmail's OAuth flow never needed them) — a Personal API Token can silently expire between requests in a way an OAuth refresh flow is designed not to, so `withConnectedClickUp()` (`lib/api/clickup.ts`) catches any ClickUp API failure on a data call and flips `status: 'error'` + `lastError` rather than leaving the row saying `connected` while every real request 401s; the Integrations page card reads `status==='error'` to show a "Connection error" badge and swap "Connect" for "Reconnect", not by inferring failure from a request that already happened.

**Permission model deviation, deliberate:** `integration:configure` (connect/disconnect, unchanged, owner/admin-only) and `integration:read` (viewing connection status / using ClickUp data) already existed from Slack/Gmail's build, but `read` was owner/admin-only too — fine while its only consumer was the fully admin-gated `/admin/integrations` page. Migration `0104_widen_integration_read.sql` grants `integration:read` to member/viewer as well, because ClickUp's Communication-pillar page (`/communication/clickup`) needs a genuine "any role can view" tier the original build never needed. No new `manage`/`view` action values were added — reusing the existing `configure`/`read` action vocabulary, per the schema's own stated rule against resource-specific action strings (`db/schema.ts:530-535`).

**Disconnect deliberately has no ClickUp-specific route** — the existing generic `DELETE /api/integrations/[id]` (used by Slack/Gmail already) clears `config`/`status`/`connectedAt` for any provider by row id with no provider-specific logic, so it works for ClickUp unmodified; the Integrations page just calls it, wrapped in a confirm dialog for ClickUp specifically (Slack/Gmail's disconnect button is untouched, per the constraint to leave them exactly as they are). Reconnecting after an error reuses the same connect route/upsert-by-`(org_id, provider)` lookup — no unique DB constraint needed, same select-then-insert-or-update pattern Slack's OAuth callback already used.

**ClickUp API client** (`lib/api/clickup.ts`) is server-only — every function takes a token as a plain argument and calls `api.clickup.com` directly with `Authorization: <token>` (ClickUp's raw-token header, no "Bearer" prefix, on both API versions used); nothing in `app/(app)/communication/clickup/page.tsx` or the Integrations page ever imports it or sees a token, only the API routes do. No list-picker exists yet for tasks: `fetchClickUpTasks()` walks the workspace's first space → first folderless list → first folder's list and reads from whichever it finds first, a deliberate simplification (spec explicitly allowed deferring list selection) rather than a missed feature — letting the user pick a list is a natural follow-up.

**ClickUp Docs — added as a read-only second pass**, once real usage surfaced the gap (task discussion alone wasn't "docs" per the original spec's schema comment). Docs live on ClickUp's **v3** API (`/api/v3/workspaces/{team_id}/docs` to list, `/api/v3/workspaces/{team_id}/docs/{doc_id}/pages?content_format=text/md` for page content) — a genuinely separate surface from the v2 endpoints (team/space/list/task/comment) everything else here uses; `clickupFetchV3()` in `lib/api/clickup.ts` is a second thin wrapper alongside `clickupFetch()`, same raw-token header, different base URL. **Docs are read-only, not a scope cut but an API ceiling**: verified directly against the live API (a doc/page id posted to the task-comment endpoint 404s) and cross-checked against the ClickUp MCP connector already available in this environment — its own `create_comment` tool only accepts `entity_type: task | list | view`, never `doc`, confirming ClickUp's public API has no doc-page-comment endpoint at all, in either version. `/communication/clickup` gained a Tasks/Docs tab switcher (same segmented-control pattern used elsewhere in this app, e.g. Recommendations' category filter) rather than a second route, since both views share one connected-workspace concept. Doc content renders as plain preformatted markdown source (not rendered HTML — no markdown renderer exists in this codebase yet, and adding one wasn't in scope for wiring the connector). One new mock function, `summarize_doc` (Analyst) — no `draft_*` counterpart, since there's nothing to reply to on a read-only page. `GET /api/integrations/clickup/docs` and `GET /api/integrations/clickup/docs/[doc_id]` are both `integration:read`-gated, same tier as the tasks/comments routes.

**Communication pillar placement:** ClickUp was added as a new sidebar tab (`/communication/clickup`, `components/AppShell.tsx`'s Communication section) alongside Messenger/Mail/Calls/Video, not as the pillar's primary tab — additive, since Messenger/Mail/Calls/Video (Slack DMs, Gmail, phone log, video meetings) are the four channels CLAUDE.md §11a actually scoped as "Communication," and ClickUp is closer in spirit to a project-management connector than a communication channel; making it primary would have implied it demotes those four, which wasn't asked for. The page follows the *existing Communication pillar's* own established pattern for AI touchpoints (inline "AI: Summarize"/"AI: Draft a reply" buttons + `AiBanner` + `Modal`, copied from `messenger/page.tsx`) rather than the `AiButton`/`AiSuggestionCard` components CLAUDE.md §7 names as canonical elsewhere (HR/CRM) — consistency with the three sibling pages in the same pillar took precedence over the cross-app-canonical pattern, since introducing a second AI-touchpoint visual language into the same four-tab pillar would read as inconsistent on the page it actually sits next to. Two new mock functions, `summarize_task_comments` (Analyst) and `draft_task_comment_reply` (Writer), in `lib/ai/mockResponses.ts`, same `generateAI()` router as everything else. `CommunicationBanner` (`components/CommunicationChrome.tsx`) gained an optional `connectHref` prop — when passed (ClickUp only), it links straight to `/admin/integrations` instead of every other Communication page's placeholder "coming soon" modal, since ClickUp's connector is the one real one.

Regression test: `db/test-clickup-integration-verify.ts` — a member without `integration:configure` is denied (403), an invalid token is rejected without writing a row, a valid token connects and stores `config` correctly, and the stored token is verified absent from `toPublicIntegration()`'s JSON output (not just assumed). `global.fetch` is stubbed to fake ClickUp's `/team` endpoint rather than hitting the real API, so the test runs without a live ClickUp account or network access — same reasoning as any external-HTTP-dependency test double.

**Messenger (`/communication/messenger`) is now backed by real ClickUp Chat — the first Communication sub-page to leave "mocked, integration lands later" behind**, replacing `mockSlack` entirely (Mail/Calls/Video are untouched, still on `lib/mock/communication.ts`). This is ClickUp's **v3 Chat API, which ClickUp itself labels experimental** — flagging this explicitly since it's the one dependency in this codebase most likely to need a follow-up patch if ClickUp changes response shapes without the usual v2 stability guarantees. Every endpoint shape below was verified against the **live API directly via curl** while building this, not assumed from ClickUp's docs, and diverged from a first read of them in three ways worth knowing before touching this code again: list endpoints wrap results in `{ data: [...], next_cursor }`, not a `channels`/`messages` key; sending a message's body key is `content`, not `message`; and creating a DM's body key is `member_ids`, not `members`. `lib/api/clickup.ts` gained a second base URL (`clickupFetchV3`, alongside the existing `clickupFetch` for v2 team/space/list/task/comment) plus:
- `fetchClickUpChatUser()` — `GET /user` (v2), the only way to identify the token's own owner, needed to know which side of a 2-person DM is "the other person" (DM-type channels carry no `name` field from the API at all).
- `fetchClickUpChatChannels()` — resolves every channel's real name (or the DM's other-participant name) and member count via a per-channel members fetch, **cached in-memory for 60s per org** (`channelCache`, module-level `Map`) per the spec's ask to avoid hammering the API on every page load. This cache is per-process — correct for this app's current single-instance shape, not correct if ever deployed across multiple serverless instances sharing no memory; noted here rather than silently assumed to scale. `postClickUpChatMessage()`/`createClickUpChatDM()` both invalidate the cache for that team, so a just-sent message's channel list (`latest_comment_at`) doesn't go stale for up to 60s.
- `fetchClickUpChatMessages()` — **always fetched fresh, never cached** (explicit spec requirement, unlike the channel list), resolves each message's `user_id` to a display name via the same channel-members endpoint, sorted ascending by `date` (the API's own ordering wasn't verified to be guaranteed, so this is enforced client-side rather than assumed).
- `postClickUpChatMessage()` / `fetchClickUpWorkspaceMembers()` (for the "+ New DM" picker, `GET /team/{team_id}` v2 — the same endpoint `validateClickUpToken` already uses) / `createClickUpChatDM()`.

**429 (rate limit) is handled as a distinct case from every other ClickUp error**, not lumped into the generic 401/other-error handling `withConnectedClickUp()` already had: a 429 is re-thrown as-is without flipping the integration's `status` to `'error'`, because a transient rate limit is not a broken connection — marking the whole integration "needs reconnect" over a rate limit would be actively wrong, unlike an actually-invalid/revoked token where that's the correct response. The route surfaces the 429's message to the UI; the Integrations page's card is left alone.

UI: the existing Messenger two-column shell (`app/(app)/communication/messenger/page.tsx`) is unchanged in structure — only its data source changed, per the spec's explicit "swap the data source, not a rebuild" framing. This is also why it keeps the pillar's existing inline-AI-button pattern (`AiBanner` + plain buttons) rather than adopting `AiButton`/`AiSuggestionCard` — consistency with the sibling Mail/Calls/Video pages in the same pillar (and with the just-built ClickUp task-discussion UI) took precedence over introducing a second AI-touchpoint visual language into one four/five-tab pillar, same reasoning already documented for the ClickUp Docs tab above. `summarize_channel`'s mock (and its real-provider prompt in `lib/ai/prompts.ts`) now derives its output from the actual passed-in message transcript instead of a hardcoded fictional narrative about a PR merge — "genuinely relevant to real content" applies to the mock path too, not just the real-LLM path. `draft_slack_reply` was left as-is (key name and mock logic both) since it already worked off real message content (`ctx.preview`, the channel's last message) even before this change — spec explicitly allowed keeping the key.

Regression test: `db/test-clickup-chat-verify.ts` — a role with no seeded `integration:read` grant is denied (403; note every *built-in* role owner/admin/member/viewer already has `integration:read` since migration 0104, so this test uses a custom, never-granted role name rather than `member`, which would now incorrectly pass), sending a message to a never-connected org throws before any network call happens, and the token is verified absent from the real channel/message response shapes (stubbed against the exact live-API shapes captured above, not idealized ones).

**Google Meet is the third real integration and the first OAuth connector added since Gmail — extending, not duplicating, Gmail's already-working OAuth plumbing.** Part 0 found Gmail (`lib/api/gmail.ts`) already had a fully working Google OAuth flow using `GOOGLE_CLIENT_ID`/`GOOGLE_CLIENT_SECRET`, and confirmed the `integrations` table's provider enum did **not** already have `google_meet` (a spec assumption that didn't match reality, same class of gap as ClickUp's Part 0) — added via migration `0105_add_google_meet_provider.sql`, no new columns, same deviation-from-a-literal-schema-request reasoning as ClickUp: `access_token`/`refresh_token`/`expires_at` (ISO string)/the connected email (as `team_name`, reused generically)/`calendar_id` all live in the existing plaintext `config` jsonb column, not dedicated typed columns — consistent with Slack/Gmail/ClickUp, and `toPublicIntegration()` already strips `config` before any response regardless of what's inside it.

**Shared OAuth logic was extracted, not duplicated**: `lib/api/googleOAuth.ts` is a new module holding `googleAuthorizeUrl()` (now takes `scope` as a parameter instead of Gmail hardcoding its own), `exchangeGoogleCode()`, `refreshGoogleAccessToken()`, and a new `revokeGoogleToken()` — `lib/api/gmail.ts` was refactored to import these instead of keeping its own copies (behavior-preserving; `gmailAuthorizeUrl`/`exchangeGoogleCode` are still exported from `gmail.ts` for its existing route imports, just thin wrappers now). This is why the header comment in `lib/api/gmail.ts` about "this could later extend to other Google products" undersold what actually happened — Gmail came first, Google Meet is the one that arrived second and triggered the extraction.

**Redirect URI path deviates from a literal env var the spec proposed.** The spec listed `GOOGLE_REDIRECT_URI=.../api/integrations/google/callback` as an env var to add; the established Gmail pattern instead *derives* the redirect URI from `req.nextUrl.origin` at request time (`${origin}/api/integrations/google/callback`) rather than reading a separate env var — avoids a second source of truth that could silently drift from the actual deploy origin (a mismatch there is a common, confusing OAuth failure mode). No `GOOGLE_REDIRECT_URI` env var was added; only the already-existing `GOOGLE_CLIENT_ID`/`GOOGLE_CLIENT_SECRET` are needed, same two Gmail already uses. **Whoever registers this OAuth client in Google Cloud Console needs to add `.../api/integrations/google/callback` as an additional Authorized redirect URI** (for both the production origin and `http://localhost:3000` for local dev) alongside Gmail's existing `.../api/integrations/gmail/callback` — same client, two redirect URIs now.

**Route naming**: connect/callback/disconnect/meetings all live under `app/api/integrations/google/*` (not `google-meet/*`) — matching the spec's explicit route list for Part 2/3, and distinct from the old `app/api/integrations/google-meet/connect/route.ts` **stub, which was deleted** (it always redirected with "lands with the real connector" — now it exists, so the stub was superseded, not left alongside). The Integrations page's provider catalog key is `google_meet` (matching the DB enum, for consistent lookups), but its `connectRoute` field points at the `google` URL segment — the one provider here where the catalog key and the route folder name intentionally differ, documented inline in `page.tsx` since every other provider's key and route folder are identical and a silent exception would be easy to miss.

**Token refresh is expiry-aware, a deliberate difference from Gmail's simpler model.** Gmail's `sendGmailMessage()` refreshes unconditionally before every send (fine — sends are rare, and gmail.send has no cheaper path). Google Meet's Calendar API gets hit far more often (every Video-tab load lists meetings), so `getValidGoogleToken()` (`lib/api/googleMeet.ts`, exported specifically so `db/test-google-meet-integration-verify.ts` can call it directly) checks the stored `expires_at` first and only calls Google's refresh endpoint when the token is actually expired or within 60s of expiring — verified directly in the regression test (a fresh token triggers zero refresh calls; a manually-expired one triggers exactly one). `withGoogleCalendar()` layers one more thing on top: if Google still returns 401 despite our stored expiry looking fine (clock drift, or the token was invalidated out-of-band), it forces one refresh and retries the call exactly once before giving up — the user should never see a raw 401, per spec. Only a failed *refresh* (the `refresh_token` itself being invalid/revoked, e.g. the user pulled access from `myaccount.google.com/permissions`) sets `status: 'error'` and prompts reconnect; a single transient Calendar API failure that survives the one retry does not, mirroring the 429-vs-other-errors distinction already made for ClickUp.

**Disconnect is a dedicated route** (`POST /api/integrations/google/disconnect`), unlike ClickUp's reuse of the generic `DELETE /api/integrations/[id]` — Google actually has a revoke endpoint worth calling (a Personal API Token has no equivalent), so disconnecting here is a real outbound API call (best-effort — a already-invalid token 400s from Google's revoke endpoint harmlessly, the stored copy is cleared either way), not just a DB clear.

**Video Conferencing (`/communication/video`) keeps its existing shell** (Upcoming/Past tabs, List/Week toggle, the 7-day `WeekStrip`) and swaps `mockZoom` for real Calendar data — same "data-source swap, not a rebuild" reasoning as ClickUp's Docs tab and Messenger. Transcript/recording badges and the old "AI: Summarize meeting" touchpoint were removed outright rather than kept as fake placeholders, since this integration has no transcript data to summarize (Google Meet doesn't expose transcripts/recordings through the Calendar API this integration uses) — **repurposed as "AI: Draft meeting notes template"** (Writer agent, `draft_meeting_notes_template` in `lib/ai/mockResponses.ts`/`lib/ai/prompts.ts`): generates a blank-sections markdown template (Agenda/Discussion notes/Decisions/Action items) from the meeting's real title and attendees, explicitly not fabricated meeting content, for the user to fill in themselves. The old past-meeting "view details" modal (which only ever showed participants + a transcript preview) was removed rather than kept mostly-empty — its one still-relevant action (open the notes template) is now directly on every meeting card and via `WeekStrip` pill clicks, both tabs. **The Dashboard's "Next Meeting" card and the Communication hub page still read from `lib/mock/communication.ts`'s `nextMeeting()`/`mockZoom` unchanged** — out of scope per the prompt (only Video Conferencing was in scope), left exactly as-is, same as the ClickUp work left the Dashboard's other mocked Communication stats alone.

Regression test: `db/test-google-meet-integration-verify.ts` — a caller without `integration:configure` is denied even when supplying a real `org_id` as the OAuth `state` value (state is never trusted as proof of access on its own), neither `access_token` nor `refresh_token` ever appears in `toPublicIntegration()`'s output, and `getValidGoogleToken()` is verified to skip refreshing a fresh token (zero refresh calls) and to refresh exactly once for a manually-expired one — `global.fetch` stubbed against Google's real token/userinfo/revoke endpoint shapes, same reasoning as every other external-HTTP-dependency test double in this repo.

## 11. Phase Gate Rule

Do not start a phase until the prior phase's acceptance criteria pass. Confirm completion with Urvil before moving forward. If a paid-tier substitution becomes unavoidable, flag it explicitly rather than silently switching — same rule as Recur8's Phase 3 gate with Chintan.
